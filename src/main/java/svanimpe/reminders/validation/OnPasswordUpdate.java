package svanimpe.reminders.validation;

import javax.validation.groups.Default;

/*
 * Validation group used when a user's password was changed.
 */
public interface OnPasswordUpdate extends Default
{
}
